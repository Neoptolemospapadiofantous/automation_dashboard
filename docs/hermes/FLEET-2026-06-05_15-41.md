---
date: 2026-06-05
type: fleet
baseline_overall: WARN
final_overall: WARN
agents: [route-auditor, inertia-page-scanner, migration-watcher, voiceflow-surface-sentinel, doc-syncer]
tags: [hermes, fleet]
---
# Fleet Run — 2026-06-05 15:41 UTC

## Baseline
WARN — 5 PASS / 0 FAIL / 1 WARN. Only warning is the long-standing Pint style debt (`data/logs/pint.log`); tests, config, routes, migrations all green.

## Agent reports

### Route Auditor
- MISSING_THROTTLE: `POST /onboarding/start` — provisions from the Voiceflow project pool; loop could exhaust it (`routes/web.php:41`).
- MISSING_THROTTLE: `POST /agents/{agent}/rotate-secret` — mints new webhook secrets; abuse can churn-DoS the inbound webhook flow (`routes/web.php:49`).
- MISSING_THROTTLE: `POST /agents/{agent}/health` — outbound HTTP to Voiceflow per call (`routes/web.php:50`).
- MISSING_THROTTLE: `POST /dashboard/tick` — broadcasts to every connected browser; spam vector (`routes/web.php:64`).
- MISSING_THROTTLE: `DELETE /knowledge/{documentID}` — proxies Voiceflow KB API; sibling routes (`/knowledge/url|file|query`) are all throttled — this one was missed (`routes/web.php:118`).
- MISSING_THROTTLE (lower severity, MEDIUM): `POST /agents`, `PUT /agents/{agent}`, `DELETE /agents/{agent}`, `PUT /current-agent`, `POST /leads`, `PATCH /leads/{lead}/status`, `POST /leads/{lead}/assign`, `DELETE /leads/{lead}`, `POST /notifications/read`.
- No MISSING_AUTH, no ORPHAN_CONTROLLER, no DEBUG_ROUTE, no UNDOCUMENTED_PUBLIC findings.
- Both custom middleware (`HandleInertiaRequests`, `RequireAgent`) are registered and in use.

### Inertia Page Scanner
- No PROP_DRIFT across the six changed pages (Agents/Show, Conversations/Index, Conversations/Show, Knowledge/Index, Onboarding/Done, LeadCard).
- No LEAKED_SECRET, no DEAD_IMPORT.
- XSS_RISK (LOW): `Conversations/Index.vue:99` uses `v-html="link.label"` — standard Laravel paginator pattern; server-generated, not user-controlled, but still an unescaped sink to watch.
- A11Y (HIGH-easy): `Knowledge/Index.vue:295` `Close ×` button missing `type="button"` (inconsistent with rest of file).
- A11Y (HIGH-easy): `Knowledge/Index.vue:193-199` URL "Name (optional)" `TextInput` had no label/aria — placeholder only.
- A11Y (LOW): `Knowledge/Index.vue:247-260` and `LeadCard.vue:65-73` use `opacity-0 group-hover:opacity-100` for Inspect/Delete — SR labels fine, but keyboard focus reveals no visual focus state.

### Migration Watcher
- No FILLABLE_DRIFT, no MISSING_CAST, no MISSING_FK_INDEX, no destructive drops across the five recent migrations (`add_billing_to_teams`, `create_credit_transactions_table`, `add_mode_to_agents`, `create_voiceflow_project_pool_table`, `create_platform_settings_table`).
- DOC_DRIFT (MEDIUM): `docs/phase-13-multitenancy.md` ER diagram for `agents` omits the new `mode` column.
- DOC_DRIFT (MEDIUM): `docs/phase-13-multitenancy.md` ER diagram for `teams` omits `plan`, `credit_balance`, `credits_renewed_at`.
- DOC_DRIFT (MEDIUM): no phase doc for the live billing surface; Phase 13 roadmap still labels billing as "later".
- DUPLICATE_TIMESTAMP (LOW): two pre-existing same-timestamp migration pairs (`2026_05_31_052119_*`, `2026_06_01_062037_*`); order is filename-sort-safe today but fragile to renames.

### Voiceflow Surface Sentinel
- No HARDCODED_SECRET, no SWALLOWED_EXCEPTION, no UNVERIFIED_WEBHOOK, no DOC_DRIFT.
- HARDCODED (LOW): `VoiceflowService.php:50-53` inlines production URL defaults as `??` fallbacks; primary source is `config/services.php`. Consider failing fast on missing config instead.
- UNSAFE_HTTP (MEDIUM): no `->retry()` on `interact` (l.146), `getVariables` (l.182), `searchTranscripts` (l.329), `getTranscript` (l.347), KB CRUD (l.405, 430, 459, 480, 501), `queryKnowledgeBase` (l.522), `health::interact` (l.299). Only `startSessionResponse` (l.645) retries — and only on ConnectionException, not 5xx.
- Webhook auth (`VoiceflowWebhookController::leadCaptured`) uses constant-time `hash_equals` against per-agent `X-Webhook-Secret` — correct given Voiceflow's lack of HMAC signing for outbound Custom Action POSTs.

### Doc Syncer
Files edited directly (in-prior-run):
- `docs/architecture/integration-map.md` — corrected stale artisan signature `vf:transcripts:backfill` → `voiceflow:backfill`.
- `docs/README.md` — removed "modes (BYOK vs managed)" from the Phase 13 index entry (BYOK was dropped from the product surface).
- `docs/phase-14-public-stats.md` — moved shipped fields (`messages_last_24h`, `time_saved_hours`, `last_activity_at`) out of "next ideas".
- `docs/phase-12-knowledge-base.md` — moved shipped file-upload + delete out of "next ideas".
- `docs/phase-13-multitenancy.md` — added auto-synced note for Phase E (BYOK removal) + replaced obsolete 5-state onboarding diagram with current 3-state.
- `docs/phase-11-transcript-backfill.md` — replaced obsolete `/agent` references with `/chat`, pointed at Phase 14 rename.
Findings carried forward:
- UNDOCUMENTED (MEDIUM): billing surface (Plan rebrand + credit top-ups + invariants) shipped across commits 0b9d8e4 → 932d340 but no `phase-H-billing.md` exists; Phase 13 still lists Billing as "later".
- UNDOCUMENTED (MEDIUM): per-agent KB UX (`00c83fd`) — phase-12 patched with a pointer, but no dedicated doc.
- STALE (LOW): `phase-13-multitenancy.md` "Out of scope" still asserts BYOK-only stance; left intact as historical record.

## Actions taken
- `routes/web.php:41` — added `throttle:5,1` to `POST /onboarding/start` (pool-exhaustion gate).
- `routes/web.php:49` — added `throttle:5,1` to `POST /agents/{agent}/rotate-secret` (secret-churn gate).
- `routes/web.php:50` — added `throttle:30,1` to `POST /agents/{agent}/health` (upstream-quota gate).
- `routes/web.php:64` — added `throttle:30,1` to `POST /dashboard/tick` (broadcast-spam gate).
- `routes/web.php:118` — added `throttle:30,1` to `DELETE /knowledge/{documentID}` (parity with sibling KB routes).
- `resources/js/Pages/Knowledge/Index.vue:193` — added `id` + `aria-label` to URL "Name" TextInput.
- `resources/js/Pages/Knowledge/Index.vue:295` — added `type="button"` to `Close ×` button.

## Verification
`bash scripts/hermes.sh --fast` → **WARN** (5 PASS / 0 FAIL / 1 WARN). Pre-existing Pint style debt is the sole warning; tests, config, routes, migrations all green. Route changes did not break route registration (`PASS routes`).

## Carry forward
- MEDIUM: Add `->retry(2, 200, when: ConnectionException only)` to hot-path Voiceflow calls (`interact`, `getVariables`, `queryKnowledgeBase`, `searchTranscripts`, `getTranscript`).
- MEDIUM: Throttle the remaining auth-only mutating routes (`/agents` CRUD, `/leads` CRUD, `/notifications/read`, `PUT /current-agent`) — abuse-tier rate limit is enough.
- MEDIUM: Refresh `docs/phase-13-multitenancy.md` ER diagrams to include `agents.mode` and the three new `teams` billing columns.
- MEDIUM: Write a `phase-H-billing.md` (Plan rebrand, top-up flow, CreditMeter invariants); update Phase 13 roadmap to reflect that Billing has shipped.
- MEDIUM: Move Voiceflow URL fallbacks out of `VoiceflowService.php:50-53` into `config/services.php` (or fail fast on missing config).
- LOW: Keyboard-focus state for `opacity-0 group-hover:opacity-100` action buttons (`Knowledge/Index.vue:247-260`, `LeadCard.vue:65-73`).
- LOW: Revisit `v-html="link.label"` in `Conversations/Index.vue:99` if paginator labels ever start carrying user-controlled text.
- LOW: Rename one half of each duplicate-timestamp migration pair (`2026_05_31_052119_*`, `2026_06_01_062037_*`) to remove sort-order fragility.
