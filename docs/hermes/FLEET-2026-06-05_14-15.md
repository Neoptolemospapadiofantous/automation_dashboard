---
date: 2026-06-05
type: fleet
baseline_overall: WARN
final_overall: WARN
agents: [route-auditor, inertia-page-scanner, migration-watcher, voiceflow-surface-sentinel, doc-syncer]
tags: [hermes, fleet]
---
# Fleet Run — 2026-06-05 14:15 UTC

## Baseline
WARN — 5 PASS / 0 FAIL / 1 WARN. Sole warning is pre-existing Pint style debt across ~50 files (`data/logs/pint.log`); tests, config, routes, migrations all green.

## Agent reports

### Route Auditor
- MISSING_THROTTLE: `POST /api/voiceflow/lead-captured/{agent:slug}` — public webhook authenticated only by `X-Webhook-Secret`; no rate limit (`routes/api.php:16`).
- MISSING_THROTTLE: `POST /chat/launch`, `POST /chat/interact` — billable Voiceflow calls (`routes/web.php:86-87`).
- MISSING_THROTTLE: `POST /knowledge/url`, `POST /knowledge/file`, `POST /knowledge/query` — external Voiceflow KB API (`routes/web.php:100-102`).
- MISSING_THROTTLE: `POST /billing/topup` — instant credit grant in dev mode, double-submit risk (`routes/web.php:57`).
- MISSING_THROTTLE: assorted other auth-only mutating endpoints (`/agents`, `/leads`, `/notifications/read`, etc.). Lower severity, recorded only.
- UNDOCUMENTED_PUBLIC: `POST /api/voiceflow/lead-captured/{agent:slug}` not listed in `docs/public-surface.md` despite framework-level public exposure.
- UNDOCUMENTED_PUBLIC: `GET /` (framework Welcome) — benign carve-out, not in public-surface table.
- No MISSING_AUTH, ORPHAN_CONTROLLER, or DEBUG_ROUTE findings.

### Inertia Page Scanner
- DEAD_IMPORT: `resources/js/Pages/Knowledge/Index.vue:8` — `SecondaryButton` imported, never used.
- A11Y: `resources/js/Components/LeadCard.vue:65-72` — delete button has only `✕` glyph + `title`; no `aria-label`.
- A11Y: `resources/js/Components/LeadCard.vue:77-86` — assignment `<select>` has no `<label>` or `aria-label`.
- XSS_RISK (low): `resources/js/Pages/Conversations/Index.vue:99` — `v-html="link.label"` on Laravel paginator. Server-controlled, low practical risk; standing hazard.
- No PROP_DRIFT, no leaked secrets, no hardcoded URLs/keys across the six changed pages/components.

### Migration Watcher
- DUPLICATE_TIMESTAMP: `2026_06_01_062037_create_conversations_table.php` and `..._create_messages_table.php` share the same timestamp. Alphabetical order resolves correctly today; rename fragility.
- DUPLICATE_TIMESTAMP: `2026_05_31_052119_create_personal_access_tokens_table.php` and `..._create_teams_table.php` share a timestamp. Independent tables, ordering doesn't matter.
- MISSING_FK_INDEX: `teams.current_agent_id` (`2026_06_03_000001_add_current_agent_id_to_teams.php`).
- MISSING_FK_INDEX: `credit_transactions.agent_id` (`2026_06_03_100001_create_credit_transactions_table.php`).
- MISSING_FK_INDEX: `voiceflow_project_pool.assigned_to_agent_id` (`2026_06_03_220000_create_voiceflow_project_pool_table.php`). Touched on every agent delete via `PoolAllocator::release`.
- CLEAN: fillable/casts (`Agent::mode`, `Team::{plan,credit_balance,credits_renewed_at}`, all JSON-casted columns), no destructive changes, no orphan models, phase-13/14 ERDs match real schema.

### Voiceflow Surface Sentinel
- CLEAN. All hosts come from `config('services.voiceflow.*')`; all `Http::*` calls set `connectTimeout`+`timeout` and `->throw()`/`->successful()`. `interact()` deliberately omits retry. Webhook secret compared with `hash_equals()` + empty-secret 503 guard. JSON decode paths defensively typed. Documented-but-unhandled inbound surfaces (`runtime.session.*`, `runtime.call.*`, `organization.project.*`) are explicitly catalogued as "not used" in `docs/voiceflow/README.md` — by design, not drift.

### Doc Syncer
- Edited directly:
  - `docs/public-surface.md` — added `messages_last_24h`, `time_saved_hours`, `last_activity_at` to JSON example + field table; expanded `display.*`.
  - `docs/architecture/public-stats-flow.md` — added 24h-messages + last-qualified-lead queries to the cache-miss diagram; expanded the "Why these counts" list to all 7 counts + activity timestamp.
- STALE (left as-is per "phase docs are immutable history" convention):
  - `docs/phase-13-multitenancy.md` Phase E (BYOK wizard removed in 0ede0da).
  - `docs/phase-13-multitenancy.md` Phase D (`UpdateAgentCredentials` no longer reachable from UI after 0ede0da; routes still exist).
  - `docs/phase-12-knowledge-base.md` "Next ideas" (file upload + delete shipped in 00c83fd).
  - `docs/phase-14-public-stats.md` "Next ideas" (all three shipped in ffe948e).
- UNDOCUMENTED (no successor phase doc):
  - BYOK drop (0ede0da), credit top-up + Custom-cap removal (e13a019, 0b9d8e4), per-agent KB UX (00c83fd), public stats v2 (ffe948e), cross-page interconnections (58fbd68 — architecturally documented in `integration-map.md`), billing display-math + zero-balance invariants (ee96da6, 425fe4d).
- OK: `docs/README.md`, `docs/architecture/integration-map.md`, `docs/architecture/landing-sse-pipeline.md`, phase docs 2/3/4/5/6/7/11, `docs/typesense-setup.md` — all referenced files exist.

## Actions taken
- `routes/api.php:16` — added `throttle:60,1` to the Voiceflow lead-captured webhook.
- `routes/web.php:58` — added `throttle:10,1` to `billing.topup` (double-submit + credit-grant guard).
- `routes/web.php:87-90` — added `throttle:30,1` to `chat.launch` and `throttle:60,1` to `chat.interact`.
- `routes/web.php:103-110` — added `throttle:30,1` / `20,1` / `60,1` to `knowledge.url` / `knowledge.file` / `knowledge.query` respectively.
- `docs/public-surface.md` — replaced the "only unauthenticated endpoint" framing with a table listing all three non-session-authenticated routes (anonymous stats, secret-authed Voiceflow webhook, framework Welcome).
- `resources/js/Pages/Knowledge/Index.vue:8` — removed unused `SecondaryButton` import.
- `resources/js/Components/LeadCard.vue:69` — added `aria-label="Delete lead"` to the icon-only delete button.
- `resources/js/Components/LeadCard.vue:80` — added `aria-label="Assign lead"` to the assignment `<select>`.
- The doc-syncer agent's edits to `docs/public-surface.md` + `docs/architecture/public-stats-flow.md` (public-stats contract fields) also stand.

## Verification
Hermes re-run after fixes: WARN — 5 PASS / 0 FAIL / 1 WARN. Tests still green. Pint warning is the same pre-existing style debt across ~50 files; the routes/web.php edit lands on already-Pint-warned lines (`fully_qualified_strict_types`, `ordered_imports`) and does not introduce new fixers.

## Carry forward
- **MEDIUM** — DB indexes on three FKs: `teams.current_agent_id`, `credit_transactions.agent_id`, `voiceflow_project_pool.assigned_to_agent_id`. Skipped this run because the HARD DO-NOT-TOUCH list excludes `database/migrations/**`. Worth a dedicated migration when next touching the schema (notably the pool one — `PoolAllocator::release` will full-scan on every agent delete as the pool grows).
- **MEDIUM** — Auth-only mutating endpoints (`/agents/*`, `/leads/*`, `/notifications/read`, `/dashboard/tick`, `/onboarding/start`, `/current-agent`) have no rate-limit middleware. Per-tenant DoS surface; consider a default-throttle group.
- **MEDIUM** — Duplicate migration timestamps (`2026_06_01_062037`, `2026_05_31_052119`). No current ordering bug; rename one of each pair next time the migration set gets touched.
- **MEDIUM** — `v-html` on Laravel paginator labels in `Conversations/Index.vue:99`. Replace with a tiny `‹ / ›` renderer or `v-text` if labels are predictable.
- **LOW** — Phase docs for BYOK drop, credit top-up, per-agent KB UX, public-stats v2 are missing. Per project convention, phase docs are immutable — a Phase 15 (or similar) successor doc could collate these.
- **LOW** — Pint style debt (~50 files). Single `vendor/bin/pint` run would clear, but cuts wide and would noise up history.
